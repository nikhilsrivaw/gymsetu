# Claude Code
.claude/

# Expo
.expo/
app/.expo/
app/dist/
app/web-build/

# Dependencies
app/node_modules/
node_modules/

# Environment files (contain secrets)
app/.env
app/.env*.local
.env
.env*.local

# Native build folders
app/ios/
app/android/

# Build artifacts
app/expo-env.d.ts
app/*.tsbuildinfo
*.tsbuildinfo

# Native signing
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
.kotlin/

# Metro
.metro-health-check*

# Debug logs
npm-debug.*
yarn-debug.*
yarn-error.*

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Website lives in its own git repo — kept here locally for reference only
GYMSETU--WEB/

# Markdown / planning docs (not code) — but DO track the backend runbooks
*.md
!database/*.md
!deploy/*.md

# Database folder is now the AUTHORITATIVE, deployable backend — DO track it.
# (Previously ignored as "schema reference only"; reversed after the Supabase
#  project was deleted and rebuilt from these files.)